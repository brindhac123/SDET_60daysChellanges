class Solution {
    public String booleanOperations(boolean a, boolean b) {
        // Code here
        StringBuilder sb = new StringBuilder();

        sb.append(a && b);
        sb.append(" ");
        sb.append(a || b);
        sb.append(" ");
        sb.append(!a);

        return sb.toString();
    }
}
