// User function template for Java

class Solution {
    static String delAlternate(String S) {
        // code here
        String st="";
        for(int i=0;i<S.length();i+=2){
            st+=S.charAt(i);
        }
        return st;
    }
}
