class Solution {
    public static boolean prime(int n) {
        // Write your code here
        if(n<=1)
        return false;
        int c=0;
        //if(n==1)
        for(int i=2;i<=n/2;i++){
            if(n%i==0){
                c+=1;
            }
        }
        return(c==0);
    }
}
