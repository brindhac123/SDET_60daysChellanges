
class Solution {
    public static int getMaxVal(int arr[], int k) {
        // Your code here
        Arrays.sort(arr);
        int sum=0,n=arr.length;
        for(int i=n-k;i<n;i++){
            sum+=arr[i];
        }
        return sum;
    }
}
